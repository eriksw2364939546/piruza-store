// ═══════════════════════════════════════════════════════
// Login Layout — без sidebar и header
// Перекрывает admins-piruza/layout.js только для /login
// ═══════════════════════════════════════════════════════

const LoginLayout = ({ children }) => {
    return children;
};

export default LoginLayout;